import { ChevronLeft, ChevronRight, CircleAlert, Eye, RefreshCw, Search, UsersRound } from "lucide-react"
import { useCallback, useEffect, useState, type FormEvent, type ReactElement } from "react"
import { useNavigate, useSearchParams } from "react-router-dom"

import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Skeleton } from "@/components/ui/skeleton"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { isSuperAdmin } from "@/features/auth/auth-access"
import { useAuth } from "@/features/auth/auth-context"
import { getCustomer, getCustomers } from "@/features/customers/customer-service"
import type { CustomerListResponse, CustomerRecord } from "@/features/customers/customer.types"
import { getPartners } from "@/features/partners/partner-service"
import type { PartnerRecord } from "@/features/partners/partner.types"
import { ApiError } from "@/lib/api-client"

function positive(value: string | null, fallback: number): number { const parsed = Number(value); return Number.isInteger(parsed) && parsed > 0 ? parsed : fallback }
function formatDate(value: string): string { return new Intl.DateTimeFormat("id-ID", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)) }

function CustomerDetail({ customer, partner, onClose, onUnauthorized, onForbidden }: { readonly customer: CustomerRecord; readonly partner: string; readonly onClose: () => void; readonly onUnauthorized: () => void; readonly onForbidden: () => void }): ReactElement {
  const { token } = useAuth(); const [detail, setDetail] = useState<CustomerRecord | null>(null); const [error, setError] = useState("")
  useEffect(() => { if (!token) return; getCustomer(token, customer.id).then(setDetail).catch((caught: unknown) => { if (caught instanceof ApiError && caught.status === 401) return onUnauthorized(); if (caught instanceof ApiError && caught.status === 403) return onForbidden(); setError(caught instanceof ApiError ? caught.message : "Tidak dapat terhubung ke server.") }) }, [customer.id, onForbidden, onUnauthorized, token])
  const item = detail ?? customer
  return <Dialog open onOpenChange={(open) => !open && onClose()}><DialogContent><DialogHeader><DialogTitle>{item.name || `Customer #${item.id}`}</DialogTitle><DialogDescription>Data Customer bersifat read-only dan berasal dari alur desktop.</DialogDescription></DialogHeader>{error ? <p role="alert" className="text-sm text-destructive">{error}</p> : <dl className="grid gap-4 sm:grid-cols-2"><div><dt className="text-xs text-muted-foreground">Nama</dt><dd className="mt-1 font-medium">{item.name || "Tidak diisi"}</dd></div><div><dt className="text-xs text-muted-foreground">Partner</dt><dd className="mt-1 font-medium">{partner}</dd></div><div><dt className="text-xs text-muted-foreground">Telepon</dt><dd className="mt-1 font-medium">{item.phone || "Tidak diisi"}</dd></div><div><dt className="text-xs text-muted-foreground">Email</dt><dd className="mt-1 break-all font-medium">{item.email || "Tidak diisi"}</dd></div><div><dt className="text-xs text-muted-foreground">Photo Session</dt><dd className="mt-1 font-medium">{item.photo_sessions_count}</dd></div><div><dt className="text-xs text-muted-foreground">Terdaftar</dt><dd className="mt-1 font-medium">{formatDate(item.created_at)}</dd></div></dl>}<DialogFooter><Button variant="outline" onClick={onClose}>Tutup</Button></DialogFooter></DialogContent></Dialog>
}

export function CustomerListPage(): ReactElement {
  const navigate = useNavigate(); const [params, setParams] = useSearchParams(); const { token, user, logout } = useAuth(); const superAdmin = isSuperAdmin(user)
  const page = positive(params.get("page"), 1); const search = params.get("search") ?? ""; const partnerId = positive(params.get("partner_id"), 0)
  const [response, setResponse] = useState<CustomerListResponse | null>(null); const [partners, setPartners] = useState<ReadonlyArray<PartnerRecord>>([]); const [state, setState] = useState<"loading" | "success" | "error">("loading"); const [error, setError] = useState(""); const [retry, setRetry] = useState(0); const [detail, setDetail] = useState<CustomerRecord | null>(null)
  const update = useCallback((values: Readonly<Record<string, string | null>>) => setParams((current) => { const next = new URLSearchParams(current); for (const [key, value] of Object.entries(values)) { if (value) next.set(key, value); else next.delete(key) } return next }, { replace: true }), [setParams])
  const unauthorized = useCallback(async () => { await logout(); navigate("/login", { replace: true }) }, [logout, navigate]); const forbidden = useCallback(() => navigate("/admin/forbidden", { replace: true, state: { from: "/customers" } }), [navigate])

  useEffect(() => { if (!token) return; const accessToken = token; const controller = new AbortController(); async function load(): Promise<void> { setState("loading"); setError(""); try { const [customers, partnerResult] = await Promise.all([getCustomers(accessToken, { search: search || undefined, partner_id: superAdmin && partnerId ? partnerId : undefined, page }, controller.signal), superAdmin ? getPartners(accessToken, { per_page: 100 }, controller.signal) : Promise.resolve(null)]); if (controller.signal.aborted) return; if (page > Math.max(1, customers.meta.last_page)) { update({ page: customers.meta.last_page > 1 ? String(customers.meta.last_page) : null }); return } setResponse(customers); setPartners(partnerResult?.data ?? []); setState("success") } catch (caught: unknown) { if (controller.signal.aborted) return; if (caught instanceof ApiError && caught.status === 401) return void unauthorized(); if (caught instanceof ApiError && caught.status === 403) return forbidden(); setResponse(null); setError(caught instanceof ApiError ? caught.message : "Tidak dapat terhubung ke server."); setState("error") } } void load(); return () => controller.abort() }, [forbidden, page, partnerId, retry, search, superAdmin, token, unauthorized, update])

  function submitSearch(event: FormEvent<HTMLFormElement>): void { event.preventDefault(); const value = new FormData(event.currentTarget).get("search"); update({ search: typeof value === "string" ? value.trim() || null : null, page: null }) }
  const partnerName = (id: number | null): string => id ? partners.find((partner) => partner.id === id)?.company_name ?? `Partner #${id}` : "Tanpa Partner"
  const filtered = Boolean(search || (superAdmin && partnerId))
  return <div className="min-w-0 space-y-6 p-4 sm:p-6 lg:p-8"><header><h1 className="text-3xl font-semibold tracking-tight">Customers</h1><p className="mt-1 text-sm text-muted-foreground">Data Customer yang dicatat melalui aplikasi desktop. Halaman ini hanya dapat dibaca.</p></header>{state === "loading" && <Skeleton className="h-[32rem]" />}{state === "error" && <Card><CardContent className="grid min-h-64 place-items-center text-center"><div><CircleAlert className="mx-auto size-10 text-destructive" /><p className="mt-3 font-medium">Customers gagal dimuat</p><p className="mt-1 text-sm text-muted-foreground">{error}</p><Button className="mt-4" variant="outline" onClick={() => setRetry((value) => value + 1)}><RefreshCw aria-hidden="true" /> Coba lagi</Button></div></CardContent></Card>}{state === "success" && <Card><CardHeader className="gap-4 border-b"><div><CardTitle>Daftar Customer</CardTitle><CardDescription>Customer anonim tidak muncul karena sesi tanpa identitas tidak membuat record Customer.</CardDescription></div><div className={`grid gap-3 ${superAdmin ? "sm:grid-cols-[minmax(14rem,1fr)_minmax(12rem,1fr)_auto]" : "sm:grid-cols-[minmax(14rem,1fr)_auto]"}`}><form key={search} className="flex gap-2" onSubmit={submitSearch}><Label className="sr-only" htmlFor="customer-search">Cari Customer</Label><Input id="customer-search" name="search" defaultValue={search} placeholder="Nama, telepon, atau email" /><Button type="submit" size="icon" variant="outline" aria-label="Cari"><Search aria-hidden="true" /></Button></form>{superAdmin && <Select value={partnerId ? String(partnerId) : "all"} onValueChange={(value) => value && update({ partner_id: value === "all" ? null : value, page: null })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Semua Partner</SelectItem>{partners.map((partner) => <SelectItem key={partner.id} value={String(partner.id)}>{partner.company_name}</SelectItem>)}</SelectContent></Select>}<Button variant="ghost" disabled={!filtered} onClick={() => setParams(new URLSearchParams(), { replace: true })}>Reset</Button></div></CardHeader><CardContent className="px-0">{!response?.data.length ? <div className="grid min-h-64 place-items-center text-center"><div><UsersRound className="mx-auto size-10 text-muted-foreground" /><p className="mt-3 font-medium">{filtered ? "Customer tidak ditemukan" : "Belum ada Customer"}</p></div></div> : <><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead>Telepon</TableHead><TableHead>Email</TableHead>{superAdmin && <TableHead>Partner</TableHead>}<TableHead className="text-right">Photo Session</TableHead><TableHead>Terdaftar</TableHead><TableHead className="text-right">Detail</TableHead></TableRow></TableHeader><TableBody>{response.data.map((customer) => <TableRow key={customer.id}><TableCell className="font-medium">{customer.name || `Customer #${customer.id}`}</TableCell><TableCell>{customer.phone || "—"}</TableCell><TableCell>{customer.email || "—"}</TableCell>{superAdmin && <TableCell>{partnerName(customer.partner_id)}</TableCell>}<TableCell className="text-right tabular-nums">{customer.photo_sessions_count}</TableCell><TableCell>{formatDate(customer.created_at)}</TableCell><TableCell className="text-right"><Button size="icon-sm" variant="ghost" aria-label={`Detail Customer ${customer.id}`} onClick={() => setDetail(customer)}><Eye aria-hidden="true" /></Button></TableCell></TableRow>)}</TableBody></Table><div className="flex flex-wrap items-center justify-between gap-3 border-t px-4 pt-4"><p className="text-sm text-muted-foreground">{response.meta.from ?? 0}–{response.meta.to ?? 0} dari {response.meta.total} Customer</p><div className="flex gap-2"><Button size="sm" variant="outline" disabled={page <= 1} onClick={() => update({ page: page - 1 === 1 ? null : String(page - 1) })}><ChevronLeft aria-hidden="true" /> Sebelumnya</Button><Button size="sm" variant="outline" disabled={page >= response.meta.last_page} onClick={() => update({ page: String(page + 1) })}>Berikutnya <ChevronRight aria-hidden="true" /></Button></div></div></>}</CardContent></Card>}{detail && <CustomerDetail customer={detail} partner={superAdmin ? partnerName(detail.partner_id) : user?.partner?.company_name ?? "Partner"} onClose={() => setDetail(null)} onUnauthorized={() => void unauthorized()} onForbidden={forbidden} />}</div>
}
